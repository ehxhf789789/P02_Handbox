//! Agent Runtime — agents are special tools that can use other tools.

/// Phase 0 stub: agent runtime placeholder.
#[derive(Default)]
pub struct AgentRuntime;

impl AgentRuntime {
    pub fn new() -> Self {
        Self
    }
}
