# Visual Claude Code: Claude Code를 상회하는 차세대 AI 개발 플랫폼

## 핵심 비전

**"Claude Code 수준의 성능 + 모델 자유 + 시각화 + 수정 가능성 + 협업 + 재사용성"**

### Handbox의 핵심 목적

```
┌─────────────────────────────────────────────────────────────────┐
│                     HANDBOX CORE MISSION                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. Claude Code 수준의 성능/품질 달성                            │
│     └─ 동일한 도구, 동일한 에이전트 루프, 동일한 결과            │
│                                                                  │
│  2. 모델 자유 (Model Freedom)                                    │
│     └─ LLM 프로바이더/모델을 자유롭게 교체 가능                  │
│     └─ Claude, GPT, Gemini, Llama, Mistral, DeepSeek 등         │
│     └─ API Key만 바꾸면 즉시 전환                                │
│                                                                  │
│  3. 모델 비교 (Model Comparison)                                 │
│     └─ 동일 워크플로우를 다른 모델로 실행하여 비교               │
│     └─ 품질, 속도, 비용을 정량적으로 측정                        │
│     └─ 최적의 모델 조합 자동 추천                                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

Claude Code는 텍스트 기반 대화형 에이전트이며 **Anthropic 모델에 종속**됩니다.
Visual Claude Code(Handbox)는 이를 넘어:
- 모든 작업 과정을 **시각적으로 표현**
- 작업 흐름을 **수정하고 재실행** 가능
- **워크플로우로 저장**하여 재사용
- **협업 및 공유** 기능
- **멀티 에이전트 오케스트레이션**

---

## 0. 모델 자유 아키텍처 (핵심 차별점)

### 0.1 LLM 프로바이더 추상화

**Claude Code 한계**: Anthropic Claude 모델에 완전 종속

**Handbox 해결책**: 모든 LLM 프로바이더를 동일한 인터페이스로 추상화

```typescript
// 통합 LLM 프로바이더 인터페이스
interface LLMProvider {
  id: string                    // 'anthropic', 'openai', 'google', 'local'
  name: string                  // 'Anthropic Claude'
  models: ModelDefinition[]     // 사용 가능한 모델 목록

  // 통합 API
  chat(messages: Message[], options: ChatOptions): AsyncIterable<Chunk>
  complete(prompt: string, options: CompleteOptions): Promise<string>
  embed(text: string): Promise<number[]>

  // 인증
  authenticate(credentials: Credentials): Promise<boolean>

  // 상태
  getUsage(): Usage
  getQuota(): Quota
}

// 지원 프로바이더
const SUPPORTED_PROVIDERS = {
  // 클라우드 API
  anthropic: { name: 'Anthropic', models: ['claude-4-opus', 'claude-4-sonnet', 'claude-3.5-haiku'] },
  openai:    { name: 'OpenAI', models: ['gpt-4-turbo', 'gpt-4o', 'gpt-4o-mini', 'o1', 'o1-mini'] },
  google:    { name: 'Google', models: ['gemini-2.0-flash', 'gemini-1.5-pro', 'gemini-1.5-flash'] },
  aws:       { name: 'AWS Bedrock', models: ['claude-*', 'titan-*', 'llama-*'] },
  azure:     { name: 'Azure OpenAI', models: ['gpt-4', 'gpt-4-turbo'] },
  deepseek:  { name: 'DeepSeek', models: ['deepseek-chat', 'deepseek-coder'] },

  // 로컬/자체 호스팅
  ollama:    { name: 'Ollama', models: ['llama3.2', 'mistral', 'codellama', 'qwen2.5'] },
  lmstudio:  { name: 'LM Studio', models: ['*'] },
  vllm:      { name: 'vLLM', models: ['*'] },
  openrouter: { name: 'OpenRouter', models: ['*'] },
}
```

### 0.2 설정 UI - 프로바이더 전환

```
┌─────────────────────────────────────────────────────────────────┐
│                    LLM PROVIDER SETTINGS                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Active Provider: [Anthropic ▾]                                 │
│  Active Model:    [Claude 4 Opus ▾]                             │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ CONFIGURED PROVIDERS                                      │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │ ✅ Anthropic      claude-4-opus, claude-4-sonnet         │  │
│  │ ✅ OpenAI         gpt-4-turbo, gpt-4o, o1                │  │
│  │ ✅ AWS Bedrock    claude-*, llama-*                       │  │
│  │ ⚠️ Google         (API key missing)                       │  │
│  │ ✅ Ollama (Local) llama3.2, mistral, codellama           │  │
│  │ ⚪ Azure OpenAI   (Not configured)                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Quick Switch:                                                   │
│  [Claude 4 Opus] [GPT-4 Turbo] [Llama 3.2] [DeepSeek]          │
│                                                                  │
│  [Add Provider]  [Test Connection]  [Save]                      │
└─────────────────────────────────────────────────────────────────┘
```

### 0.3 모델 비교 실행 (A/B 테스트)

```
┌─────────────────────────────────────────────────────────────────┐
│                   MODEL COMPARISON MODE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Workflow: "Code Review Automation"                              │
│  Input: PR #1234 - Feature implementation                        │
│                                                                  │
│  ☑ Claude 4 Opus      ☑ GPT-4 Turbo      ☑ Llama 3.2 70B       │
│  ☐ Claude 4 Sonnet    ☑ GPT-4o           ☐ Mistral Large        │
│  ☐ Claude 3.5 Haiku   ☐ o1-mini          ☑ DeepSeek Coder       │
│                                                                  │
│  [Run Comparison]                                                │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│  RESULTS                                                         │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Model           │ Time  │ Tokens │ Cost   │ Quality │ Score ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ Claude 4 Opus   │ 15.2s │ 5,420  │ $0.52  │ ⭐⭐⭐⭐⭐│ 95/100││
│  │ GPT-4 Turbo     │ 12.8s │ 4,890  │ $0.38  │ ⭐⭐⭐⭐ │ 88/100││
│  │ GPT-4o          │  8.3s │ 4,210  │ $0.21  │ ⭐⭐⭐⭐ │ 85/100││
│  │ DeepSeek Coder  │  9.1s │ 4,520  │ $0.02  │ ⭐⭐⭐⭐ │ 82/100││
│  │ Llama 3.2 70B   │ 22.4s │ N/A    │ $0.00  │ ⭐⭐⭐  │ 75/100││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  💡 Recommendation:                                              │
│  • Best Quality: Claude 4 Opus (95/100)                         │
│  • Best Value: DeepSeek Coder ($0.02, 82/100)                   │
│  • Best Speed: GPT-4o (8.3s)                                    │
│  • Best Local: Llama 3.2 70B (Free, 75/100)                     │
│                                                                  │
│  [View Detailed Diff]  [Export Report]  [Set as Default]        │
└─────────────────────────────────────────────────────────────────┘
```

### 0.4 노드별 모델 지정

```
┌─────────────────────────────────────────────────────────────────┐
│  NODE: "Code Analysis"                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Model Override: ☑ Use specific model for this node             │
│                                                                  │
│  Model: [Claude 4 Opus ▾]                                       │
│                                                                  │
│  💡 Tip: 복잡한 분석은 고성능 모델, 단순 변환은 저렴한 모델 사용 │
│                                                                  │
│  Cost Optimization Strategy:                                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ • Planning/Analysis nodes → Claude 4 Opus (High quality)  │  │
│  │ • Code Generation nodes  → DeepSeek Coder (Cost effective)│  │
│  │ • Simple Transform nodes → Haiku/Mini (Fast & cheap)      │  │
│  │ • Embedding nodes        → text-embedding-3-small         │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  [Auto-Optimize]  [Reset to Default]                            │
└─────────────────────────────────────────────────────────────────┘
```

### 0.5 비용/성능 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│                    USAGE DASHBOARD                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  This Month: $127.45 / $200.00 budget                           │
│  ████████████████████░░░░░░░░░░ 64%                             │
│                                                                  │
│  By Provider:                                                    │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Anthropic   ████████████░░░░░░░░  $78.20 (61%)              ││
│  │ OpenAI      ████████░░░░░░░░░░░░  $42.15 (33%)              ││
│  │ DeepSeek    ██░░░░░░░░░░░░░░░░░░   $5.10 (4%)               ││
│  │ Local       ░░░░░░░░░░░░░░░░░░░░   $0.00 (0%)               ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  By Workflow:                                                    │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Code Review Bot      $45.20  (892 runs)                     ││
│  │ Doc Generator        $32.10  (234 runs)                     ││
│  │ Test Generator       $28.90  (567 runs)                     ││
│  │ Bug Analyzer         $21.25  (123 runs)                     ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  💡 Cost Optimization Suggestions:                               │
│  • Switch "Test Generator" to DeepSeek → Save $24/month         │
│  • Use Haiku for simple tasks → Save $18/month                  │
│  • Enable caching for "Doc Generator" → Save $12/month          │
│                                                                  │
│  [Apply Suggestions]  [Export Report]  [Set Alerts]             │
└─────────────────────────────────────────────────────────────────┘
```

### 0.6 모델 폴백 체인

```typescript
// 자동 폴백 설정
interface FallbackConfig {
  primary: ModelRef
  fallbacks: Array<{
    model: ModelRef
    condition: 'rate_limit' | 'error' | 'timeout' | 'cost_exceeded'
  }>
  costLimit?: number  // 이 금액 초과시 저렴한 모델로 전환
}

// 예시: 자동 폴백 체인
const fallbackChain: FallbackConfig = {
  primary: 'claude-4-opus',
  fallbacks: [
    { model: 'claude-4-sonnet', condition: 'rate_limit' },
    { model: 'gpt-4-turbo', condition: 'error' },
    { model: 'deepseek-chat', condition: 'cost_exceeded' },
    { model: 'ollama/llama3.2', condition: 'timeout' },  // 로컬 폴백
  ],
  costLimit: 1.00,  // $1 초과시 저렴한 모델로
}
```

---

## 1. Claude Code 기능 완전 구현 + 확장

### 1.1 기본 도구 (Claude Code 동등)

| Claude Code Tool | Handbox 구현 | 확장 기능 |
|------------------|--------------|-----------|
| Read | `file.read` | + 미리보기, PDF/이미지/Office 지원 |
| Write | `file.write` | + 템플릿, 버전 관리, 자동 백업 |
| Edit | `file.edit` | + 시각적 diff, 병합 도구, 실시간 편집 |
| Glob | `file.glob` | + 파일 트리 시각화, 필터 프리셋 |
| Grep | `file.grep` | + 결과 하이라이팅, 코드 네비게이션 |
| Bash | `shell.execute` | + 출력 스트리밍, 터미널 에뮬레이터 |
| WebSearch | `web.search` | + 검색 결과 카드 UI, 다중 엔진 |
| WebFetch | `web.fetch` | + HTML→MD 변환, 스크린샷 캡처 |
| Task (SubAgent) | `agent.spawn` | + 시각적 에이전트 트리, 진행률 모니터링 |

### 1.2 Claude Code를 넘어서는 확장 도구

```
추가 카테고리:
├── vision/           # 이미지 분석, OCR, 차트 인식
├── audio/            # 음성→텍스트, 텍스트→음성
├── database/         # SQL, NoSQL, 벡터 DB 직접 연결
├── cloud/            # AWS, GCP, Azure 서비스 통합
├── git/              # Git 작업 시각화, PR 자동화
├── container/        # Docker, Kubernetes 관리
├── monitoring/       # 시스템 모니터링, 알림
└── automation/       # 크론잡, 이벤트 트리거
```

---

## 2. Claude Code 상회 핵심 기능

### 2.1 실시간 트레이스 시각화

**Claude Code 한계**: 작업 과정이 텍스트 로그로만 보임

**Visual Claude Code 해결책**:
```
┌─────────────────────────────────────────────────────────────┐
│                  LIVE AGENT TRACE VIEWER                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│    [User Request]                                            │
│         │                                                    │
│         ▼                                                    │
│    ┌─────────┐     ┌─────────┐     ┌─────────┐              │
│    │ Plan    │ ──▶ │ Search  │ ──▶ │ Read    │              │
│    │ ⏱ 1.2s │     │ ⏱ 2.3s │     │ ⏱ 0.8s │              │
│    └─────────┘     └─────────┘     └─────────┘              │
│                          │               │                   │
│                          ▼               ▼                   │
│                    ┌─────────┐     ┌─────────┐              │
│                    │ Analyze │ ──▶ │ Generate│              │
│                    │ ⏱ 3.1s │     │ ⏱ 2.5s │              │
│                    └─────────┘     └─────────┘              │
│                                         │                    │
│                                         ▼                    │
│                                  [Final Response]            │
│                                                              │
│  Total: 9.9s | Tokens: 12,450 | Cost: $0.23                 │
└─────────────────────────────────────────────────────────────┘
```

**구현**:
```typescript
interface AgentTrace {
  id: string
  steps: TraceStep[]
  totalTokens: number
  totalCost: number
  duration: number
}

interface TraceStep {
  id: string
  tool: string
  input: unknown
  output: unknown
  startTime: number
  endTime: number
  status: 'running' | 'completed' | 'failed'
  children?: TraceStep[]  // 서브 에이전트
}
```

### 2.2 트레이스 → 워크플로우 변환

**Claude Code 한계**: 실행된 작업을 저장/재사용 불가

**Visual Claude Code 해결책**:
```
"Save as Workflow" 버튼 클릭 →

┌──────────────────────────────────────────┐
│  Convert Trace to Reusable Workflow      │
├──────────────────────────────────────────┤
│                                          │
│  ☑ Include all steps (5 nodes)          │
│  ☑ Parameterize inputs                  │
│  ☑ Add error handling                    │
│  ☐ Optimize parallel execution           │
│                                          │
│  Workflow Name: [code_review_workflow  ] │
│                                          │
│  [Cancel]              [Save Workflow]   │
└──────────────────────────────────────────┘
```

### 2.3 워크플로우 수정 후 재실행

**Claude Code 한계**: 실행 중간에 수정하고 재시도 불가

**Visual Claude Code 해결책**:
```
[실행 중 오류 발생]
    │
    ▼
┌─────────────────────────────────────────────┐
│  Execution Failed at Node: "file.edit"      │
├─────────────────────────────────────────────┤
│  Error: File not found                      │
│                                             │
│  Options:                                   │
│  ┌───────────────────────────────────────┐  │
│  │ 1. [Fix & Retry] - 노드 수정 후 재시도  │  │
│  │ 2. [Skip] - 이 노드 건너뛰기           │  │
│  │ 3. [Replace] - 다른 노드로 대체        │  │
│  │ 4. [Abort] - 실행 중단                │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  Fix inline:                                │
│  ┌───────────────────────────────────────┐  │
│  │ path: [/correct/path/to/file.ts    ] │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  [Cancel]              [Apply & Continue]   │
└─────────────────────────────────────────────┘
```

### 2.4 멀티 에이전트 협업

**Claude Code 한계**: 단일 에이전트, 순차 실행

**Visual Claude Code 해결책**:
```
┌─────────────────────────────────────────────────────────────┐
│              MULTI-AGENT ORCHESTRATION                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐                                             │
│  │ Supervisor  │  "Review this PR and check tests"          │
│  │  Agent      │                                             │
│  └──────┬──────┘                                             │
│         │                                                    │
│    ┌────┴────┬────────────┐                                  │
│    ▼         ▼            ▼                                  │
│ ┌──────┐ ┌──────┐    ┌──────┐                               │
│ │Code  │ │Test  │    │Doc   │                               │
│ │Review│ │Runner│    │Check │                               │
│ │Agent │ │Agent │    │Agent │                               │
│ └──┬───┘ └──┬───┘    └──┬───┘                               │
│    │        │           │                                    │
│    ▼        ▼           ▼                                    │
│ [Issues] [Results]  [Warnings]                               │
│    └────────┬───────────┘                                    │
│             ▼                                                │
│     ┌──────────────┐                                         │
│     │ Aggregator   │ → [Final Report]                        │
│     └──────────────┘                                         │
│                                                              │
│  Parallel: 3 agents | Queue: 2 tasks | Completed: 5         │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 시스템 통합 수준 (Claude Code 동등 + 확장)

### 3.1 전체 시스템 제어

```typescript
// Claude Code와 동일한 수준의 시스템 접근
const systemTools = {
  // 파일 시스템 (전체 접근)
  'file.read': { permissions: 'full_disk_access' },
  'file.write': { permissions: 'full_disk_access' },
  'file.delete': { permissions: 'with_confirmation' },

  // 프로세스 관리
  'process.spawn': { shell: true, background: true },
  'process.kill': { requirePid: true },
  'process.list': {},

  // 네트워크
  'network.request': { anyProtocol: true },
  'network.listen': { requirePort: true },
  'network.tunnel': { ssh: true, ngrok: true },

  // 패키지 관리
  'package.install': { npm: true, pip: true, cargo: true, brew: true },
  'package.uninstall': { withConfirmation: true },

  // 시스템 설정
  'system.env': { read: true, write: 'session_only' },
  'system.registry': { windows: true, macos_defaults: true },
  'system.service': { start: true, stop: true, restart: true },
}
```

### 3.2 보안 레이어 (Claude Code 상회)

```
┌─────────────────────────────────────────────────────────────┐
│                  PERMISSION CONTROL CENTER                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Current Session Permissions:                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ ✅ File Read     [All directories]                      │ │
│  │ ⚠️ File Write    [~/Projects only]                      │ │
│  │ ❌ File Delete   [Requires approval each time]          │ │
│  │ ✅ Shell Execute [npm, cargo, git allowed]              │ │
│  │ ❌ Network       [localhost only]                       │ │
│  │ ⚠️ Package Mgmt  [Install only, no uninstall]           │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  Pending Requests:                                           │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 🔔 Request: Delete file /tmp/test.log                   │ │
│  │    Reason: Cleanup temporary files                      │ │
│  │    [Allow Once] [Allow Always] [Deny] [Deny Always]     │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  [Edit Permissions]  [View Audit Log]  [Reset to Default]   │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 고급 기능 (Claude Code에 없는 것들)

### 4.1 워크플로우 마켓플레이스

```
┌─────────────────────────────────────────────────────────────┐
│                  WORKFLOW MARKETPLACE                        │
├─────────────────────────────────────────────────────────────┤
│  Search: [code review automation                    ] [🔍]   │
│                                                              │
│  ┌───────────────────┐  ┌───────────────────┐               │
│  │ 📦 PR Review Bot  │  │ 📦 Test Generator │               │
│  │ ⭐ 4.8 (234)      │  │ ⭐ 4.6 (189)      │               │
│  │ Downloads: 12.3k  │  │ Downloads: 8.7k   │               │
│  │ [Install]         │  │ [Install]         │               │
│  └───────────────────┘  └───────────────────┘               │
│                                                              │
│  ┌───────────────────┐  ┌───────────────────┐               │
│  │ 📦 Doc Generator  │  │ 📦 Bug Finder     │               │
│  │ ⭐ 4.9 (567)      │  │ ⭐ 4.7 (345)      │               │
│  │ Downloads: 23.1k  │  │ Downloads: 15.2k  │               │
│  │ [Installed ✓]     │  │ [Install]         │               │
│  └───────────────────┘  └───────────────────┘               │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 실시간 협업

```
┌─────────────────────────────────────────────────────────────┐
│  👥 Collaborators                    [Invite]  [Share Link] │
├─────────────────────────────────────────────────────────────┤
│  ● Alice (editing Node 3)                                   │
│  ● Bob (viewing)                                            │
│  ○ Charlie (offline)                                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  [Workflow Graph with live cursors]                          │
│                                                              │
│       🔴 Alice's cursor                                      │
│           ▼                                                  │
│    ┌─────────┐     ┌─────────┐     ┌─────────┐              │
│    │ Node 1  │ ──▶ │ Node 2  │ ──▶ │ Node 3  │              │
│    └─────────┘     └─────────┘     └─────────┘              │
│                         ▲                                    │
│                    🔵 Bob's cursor                           │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  💬 Chat: [Type a message...                          ] [↵] │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 AI 모델 비교 실행

```
┌─────────────────────────────────────────────────────────────┐
│              MODEL COMPARISON EXECUTION                      │
├─────────────────────────────────────────────────────────────┤
│  Run same workflow with different models:                    │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Model          │ Time   │ Tokens │ Cost   │ Quality    │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │ Claude 4 Opus  │ 12.3s  │ 4,521  │ $0.45  │ ⭐⭐⭐⭐⭐ │ │
│  │ Claude 4 Sonnet│  8.1s  │ 3,892  │ $0.23  │ ⭐⭐⭐⭐   │ │
│  │ GPT-4 Turbo    │ 10.2s  │ 4,102  │ $0.35  │ ⭐⭐⭐⭐   │ │
│  │ Claude 3 Haiku │  3.2s  │ 2,451  │ $0.05  │ ⭐⭐⭐     │ │
│  │ Local Llama    │ 25.1s  │ N/A    │ $0.00  │ ⭐⭐⭐     │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  Best for Speed: Claude 3 Haiku (3.2s)                      │
│  Best for Quality: Claude 4 Opus (⭐⭐⭐⭐⭐)                 │
│  Best Value: Claude 4 Sonnet ($0.23, ⭐⭐⭐⭐)               │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 자동 최적화

```typescript
interface WorkflowOptimizer {
  // 병렬화 자동 감지
  detectParallelizable(workflow: Workflow): ParallelGroup[]

  // 중복 노드 제거
  deduplicateNodes(workflow: Workflow): Workflow

  // 캐시 포인트 추가
  insertCachePoints(workflow: Workflow): Workflow

  // 비용 최적화 (저렴한 모델로 대체 가능한 노드 탐지)
  suggestCostOptimizations(workflow: Workflow): Optimization[]

  // 성능 병목 탐지
  identifyBottlenecks(executionTrace: Trace): Bottleneck[]
}
```

---

## 5. 기술 아키텍처

### 5.1 시스템 구조

```
┌──────────────────────────────────────────────────────────────┐
│                      VISUAL CLAUDE CODE                       │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │   Editor    │  │   Trace     │  │  Execution  │           │
│  │   Layer     │  │   Viewer    │  │   Panel     │           │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘           │
│         │                │                │                   │
│         └────────────────┼────────────────┘                   │
│                          │                                    │
│  ┌───────────────────────┴───────────────────────┐           │
│  │              ORCHESTRATION ENGINE              │           │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐         │           │
│  │  │ Planner │ │Scheduler│ │ Monitor │         │           │
│  │  └─────────┘ └─────────┘ └─────────┘         │           │
│  └───────────────────────┬───────────────────────┘           │
│                          │                                    │
│  ┌───────────────────────┴───────────────────────┐           │
│  │              TOOL EXECUTION LAYER              │           │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │           │
│  │  │ File   │ │ Shell  │ │  Web   │ │  LLM   │ │           │
│  │  │ System │ │ Exec   │ │ Fetch  │ │ Calls  │ │           │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ │           │
│  └───────────────────────┬───────────────────────┘           │
│                          │                                    │
│  ┌───────────────────────┴───────────────────────┐           │
│  │           NATIVE PLATFORM (Tauri/Rust)         │           │
│  │  ┌─────────────────────────────────────────┐  │           │
│  │  │ Full System Access | Sandboxed by User  │  │           │
│  │  └─────────────────────────────────────────┘  │           │
│  └───────────────────────────────────────────────┘           │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 핵심 모듈

```rust
// hb-orchestrator: 멀티 에이전트 오케스트레이션
pub struct Orchestrator {
    agents: Vec<Agent>,
    scheduler: Scheduler,
    trace_recorder: TraceRecorder,
}

// hb-trace: 실행 트레이스 캡처 및 시각화
pub struct TraceRecorder {
    steps: Vec<TraceStep>,
    live_stream: broadcast::Sender<TraceEvent>,
}

// hb-converter: 트레이스 ↔ 워크플로우 변환
pub struct WorkflowConverter {
    fn trace_to_workflow(&self, trace: Trace) -> Workflow;
    fn workflow_to_trace(&self, workflow: Workflow) -> Trace;
    fn optimize(&self, workflow: Workflow) -> Workflow;
}

// hb-collaboration: 실시간 협업
pub struct CollaborationServer {
    rooms: HashMap<RoomId, Room>,
    fn sync_state(&self, room: RoomId, delta: StateDelta);
    fn broadcast_cursor(&self, room: RoomId, cursor: CursorPosition);
}
```

---

## 6. 구현 로드맵

### Phase 1: 기반 (4주)
- [ ] hb-trace: 실행 트레이스 캡처
- [ ] 트레이스 뷰어 UI
- [ ] 기본 도구 144개 완성
- [ ] 실시간 트레이스 스트리밍

### Phase 2: 변환 (3주)
- [ ] 트레이스 → 워크플로우 변환기
- [ ] 워크플로우 파라미터화
- [ ] 저장/로드 시스템
- [ ] 버전 관리

### Phase 3: 수정/재실행 (3주)
- [ ] 인라인 수정 UI
- [ ] 부분 재실행 엔진
- [ ] 오류 복구 옵션
- [ ] 디버그 모드

### Phase 4: 멀티 에이전트 (4주)
- [ ] 에이전트 스폰/관리
- [ ] 병렬 실행 스케줄러
- [ ] 에이전트 통신 프로토콜
- [ ] 결과 집계기

### Phase 5: 고급 기능 (4주)
- [ ] 워크플로우 마켓플레이스
- [ ] 실시간 협업
- [ ] 모델 비교 실행
- [ ] 자동 최적화

---

## 7. 차별화 포인트 요약

| 영역 | Claude Code | Visual Claude Code (Handbox) |
|------|-------------|------------------------------|
| **LLM 모델** | Anthropic Claude 전용 | **모든 LLM 자유롭게 선택** |
| **모델 비교** | 불가 | **A/B 테스트, 품질/비용/속도 비교** |
| **노드별 모델** | 불가 | **노드마다 다른 모델 지정 가능** |
| **비용 최적화** | 수동 | **자동 모델 추천, 폴백 체인** |
| **로컬 LLM** | 불가 | **Ollama, vLLM 등 완전 지원** |
| **UI** | 텍스트 터미널 | 그래프 에디터 + 트레이스 뷰어 |
| **작업 저장** | 불가 | 워크플로우로 저장/재사용 |
| **수정** | 처음부터 재시도 | 중간부터 수정/재실행 |
| **협업** | 불가 | 실시간 멀티플레이어 |
| **에이전트** | 단일 | 멀티 에이전트 오케스트레이션 |
| **공유** | 불가 | 마켓플레이스 |
| **디버깅** | 로그 기반 | 시각적 트레이스 + 브레이크포인트 |

---

## 8. 결론

Visual Claude Code(Handbox)는 Claude Code의 **모든 기능을 포함**하면서:

### 핵심 차별점: 모델 자유

```
┌─────────────────────────────────────────────────────────────────┐
│  Claude Code                │  Handbox                          │
├─────────────────────────────┼───────────────────────────────────┤
│  Anthropic Claude 전용      │  ANY LLM Provider                 │
│  API Key 1개               │  무제한 프로바이더                 │
│  모델 비교 불가             │  A/B 테스트, 자동 최적화          │
│  벤더 종속                  │  완전한 자유                       │
└─────────────────────────────┴───────────────────────────────────┘
```

### 6가지 핵심 가치

1. **모델 자유**: Claude, GPT, Gemini, Llama 등 모든 LLM 자유롭게 선택
2. **모델 비교**: 동일 워크플로우로 모델간 품질/비용/속도 A/B 테스트
3. **시각화**: 모든 작업 과정을 실시간으로 볼 수 있음
4. **재사용성**: 한번 실행한 작업을 워크플로우로 저장
5. **수정 가능성**: 실행 중에도 수정하고 재시도
6. **비용 최적화**: 노드별 최적 모델 자동 추천, 폴백 체인

### 최종 비전

```
"Claude Code 수준의 성능을 유지하면서,
 어떤 LLM이든 사용할 수 있고,
 모든 작업을 시각화하고 재사용할 수 있는
 AI 개발 플랫폼"
```

이를 통해 단순한 "AI 코딩 어시스턴트"를 넘어 **"LLM-Agnostic AI 개발 플랫폼"**으로 진화합니다.
