/**
 * useTrace — hook for trace/evidence querying.
 * Phase 0: placeholder.
 */

export function useTrace() {
  return {
    getTraces: async (_executionId: string) => {
      // Phase 1: invoke Tauri command
      return []
    },
  }
}
